{
  "name": "@stratio/st-element-utils",
  "version": "1.0.0",
  "description": "Stratio Lit Elements utils",
  "main": "dist/index.js",
  "scripts": {
    "build": "npm run clean && tsc --outDir dist",
    "clean": "rimraf dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "7.2.0",
    "rimraf": "^3.0.2",
    "typescript": "^3.9.7"
  }
}
